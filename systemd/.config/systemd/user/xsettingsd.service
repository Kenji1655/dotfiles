[Unit]
Description=XSETTINGS daemon for GTK theme propagation
After=graphical-session.target

[Service]
ExecStart=/usr/sbin/xsettingsd -c %h/.config/xsettingsd/xsettingsd.conf
Restart=on-failure
Environment=DISPLAY=:0
Environment=XAUTHORITY=%h/.Xauthority

[Install]
WantedBy=default.target
