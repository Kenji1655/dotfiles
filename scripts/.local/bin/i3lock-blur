#!/usr/bin/env bash
set -euo pipefail

# Capture, blur, lock, then clean up the temporary image.
img="${XDG_RUNTIME_DIR:-/tmp}/i3lock-blur-${USER}.png"
scrot "$img"
convert "$img" -blur 0x8 "$img"
i3lock --nofork -i "$img" -c 1d2021
rm -f "$img"
